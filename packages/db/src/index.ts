/**
 * @vaasenk/db
 *
 * Prisma schema and database client for Vaasenk (Supabase Postgres + pgvector).
 * Schema, generated client, and seeds are added in the Sprint 0 schema step.
 */

export const DB_PACKAGE_VERSION = "0.1.0";
